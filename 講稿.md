### **專案報告：Java 訂餐平台系統**

**報告人：** [你的名字]
**學號：** [你的學號]
**時間：** 10 分鐘

---

**(第一頁：封面)**

**大家好，我是 [你的名字]。**

今天我要報告的專案是一個基於 Java 開發的模擬訂餐與外送平台。這個專案的主要目標是展示一個結構清晰、具備核心功能且經過測試的後端應用程式。

---

**(第二頁：議程)**

在接下來的十分鐘裡，我會依序介紹以下幾個部分：

1.  **專案簡介**：說明這個系統是做什麼的。
2.  **系統架構**：解釋我們是如何設計和建構這個系統的。
3.  **測試策略**：我們如何確保系統的品質。
4.  **系統展示**：實際操作核心功能。
5.  **結論與未來展望**：總結並討論未來可以擴充的方向。

---

**(第三頁：專案簡介)**

這個專案模擬了一個真實世界的外送平台。它的核心功能包括：

*   **餐廳管理**：系統可以管理多家餐廳，包含餐廳的基本資訊和菜單。
*   **下訂單**：使用者可以瀏覽餐廳菜單，並建立訂單。
*   **訂單處理**：系統會驗證訂單的有效性，並更新訂單狀態，例如從「準備中」到「外送中」。
*   **外送分配**：當訂單準備好後，系統會模擬分配外送員進行配送。

從 `src/main/java` 下的 `models` 目錄中，我們可以看到 `Order.java`, `Restaurant.java` 等核心物件，這些就是我們系統的主要商業邏輯核心。

---

**(第四頁：系統架構)**

為了讓系統易於維護和擴充，我們採用了經典的分層架構。

1.  **`pom.xml`**：首先，這是一個 Maven 專案，我們透過 `pom.xml` 來管理所有依賴和建構流程。
2.  **分層設計**：
    *   **Controllers (`controllers` package)**：負責接收外部請求，是系統的入口。`GlobalExceptionHandler.java` 則用來統一處理全域的例外狀況。
    *   **Services (`services` package)**：包含主要的商業邏輯。例如 `OrderService.java` 處理訂單相關操作，`RestaurantService.java` 處理餐廳邏輯。
    *   **Repositories (`repositories` package)**：負責數據存取。為了簡化與方便測試，目前採用的是 **內存式倉儲 (In-Memory Repository)**，如 `InMemoryOrderRepository.java`。這意味著資料只會存在於程式執行的當下，重啟後會消失。
    *   **Models (`models` package)**：定義系統中的核心資料結構，如訂單 `Order` 和餐廳 `Restaurant`。
    *   **Exceptions (`exceptions` package)**：我們也定義了自訂例外，如 `OrderValidationException`，讓錯誤處理更加精確。

---

**(第五頁：測試策略)**

品質是軟體開發中非常重要的一環。在這個專案中，我們在 `src/test/java` 目錄下撰寫了多種類型的測試。

*   **單元測試 (Unit Tests)**：我們針對 `models` 和 `services` 進行了測試，例如 `OrderTest.java` 和 `RestaurantServiceTest.java`，確保每個獨立單元的功能正確。
*   **整合測試 (Integration Tests)**：`OrderLifecycleIntegrationTest.java` 是一個很好的例子，它測試了從建立訂單到完成的整個生命週期，驗證了多個服務層之間的協作是否正常。
*   **腳本化測試**：我們還提供了 `run-simplified-tests.sh` 這樣的腳本，可以快速執行一組預定義的測試場景。

---

**(第六頁：系統展示)**

接下來，我將實際操作來展示系統的核心功能。

這個專案提供了幾個方便的腳本來展示核心功能，我現在就來執行它。

**(請在此時打開您的終端機)**

首先，我執行 `run-core-requirements.sh`。

**(執行 `bash run-core-requirements.sh`)**

當我執行這個腳本時，各位可以看到終端機輸出了系統運作的日誌。這模擬了：
1.  系統初始化，載入餐廳資料。
2.  建立一筆新的訂單。
3.  系統驗證訂單內容，並將其狀態設定為「準備中」。
4.  在一段時間後，訂單完成準備，狀態更新為「等待外送員」。
5.  最後，系統成功指派外送員，訂單狀態變為「外送中」。

這個腳本完整地展示了我們訂單處理的核心流程。

---

**(第七頁：結論與未來展望)**

總結來說，我們成功建構了一個具備訂餐核心業務邏輯的 Java 後端服務。它擁有清晰的架構、明確的職責劃分，並且透過單元測試和整合測試來確保其穩定性。

當然，這個專案還有很多可以擴充的地方：

*   **資料庫整合**：將目前的 In-Memory Repository 替換成真實的資料庫，如 MySQL 或 PostgreSQL，實現資料的永久儲存。
*   **使用者身份驗證**：加入使用者註冊、登入等功能，並區分顧客和餐廳管理員權限。
*   **前端使用者介面**：開發一個 Web 或 App 前端，讓使用者可以更直觀地操作。
*   **非同步處理**：對於像是指派外送員這類可能耗時的操作，可以改用非同步訊息佇列 (Message Queue) 來提升系統效能。

---

**(第八頁：Q&A)**

我的報告到此結束。謝謝大家的聆聽，請問有任何問題嗎？
